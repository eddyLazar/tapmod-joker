import React from 'react';
import { storiesOf } from '@storybook/react';
import HeaderContainer from './HeaderContainer';

storiesOf('molecules|HeaderContainer', module).add('basic', () => (
  <HeaderContainer />
));
